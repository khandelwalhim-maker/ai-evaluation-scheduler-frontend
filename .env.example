# URL of the deployed backend (ai-evaluation-scheduler-backend), no trailing
# slash. Leave unset for local dev -- vite.config.ts proxies /api to
# http://localhost:7860 by default, so an empty value calls same-origin
# and the dev proxy forwards it to a locally running backend.
VITE_API_BASE_URL=https://your-backend.up.railway.app
